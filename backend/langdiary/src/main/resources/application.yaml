spring:
  application:
    name: langdiary
  datasource:
    url: jdbc:postgresql://localhost:5432/langdiary_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    database: PostgreSQL
    show-sql: true